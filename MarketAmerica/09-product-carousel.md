# GIT repo
git@github.com:metalshan/fresh-th-repo-21.git

# Store
horizon-anatta.myshopify.com

# Client
Market America

# Theme Base
Horizon

# Task ID
MA-00

# Theme Name
Paul-Test-MA02

# Git Branch
feat/branch-2

# Figma
Desktop: https://www.figma.com/design/hiigVhzrIBoawVnInd7QOl/Market-America---Dev-POC?node-id=309-5409&m=dev
Mobile: https://www.figma.com/design/hiigVhzrIBoawVnInd7QOl/Market-America---Dev-POC?node-id=309-6132&m=dev

# Task
This is the task to build a Featured Collection section on the home page, which is a product carousel.
The section should have collection picker setting at the section level.
The title should be the collection name itself. The featured image by default is the collection's featured image, but can be overriden in the section settings.

For the product card, use Horizon's default product card with its theme-level block composition system (_product-card blocks). The product card elements (badge, product image, star rating, product title, price, quickview) should use theme-level blocks so the merchant can reorder them and insert app blocks in between — for example, placing an app block between the product image and the star rating. 
Follow Horizon's existing _product-card block pattern. If the default product card styles need to be modified to match the Figma design, feel free to update them — we will be using the same product card across the entire theme including PLP.

Given the figma links for both mobile and desktop for the same. Build it in responsive way that matches figma.
